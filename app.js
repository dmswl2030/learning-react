// 화살표 함수
// ES6에서 생긴 기능으로 일반 함수 작성법보다 축약해서 사용할 수 있다.
// 익명함수로 사용하려면 변수에 할당하여 사용해야한다.

// 사용 방법
// {}를 사용하지 않으면 return을 생략해도 된다.

// 일반함수
function sum(a, b) {
    return a + b;
  }
  
  // 화살표 함수
  const sum = (a, b) => a + b;